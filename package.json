{
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vitepress": "^1.6.3"
  },
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "gen:sidebar": "node scripts/generate-sidebar.js",
    "deploy": "vitepress build && gh-pages -d docs/.vitepress/dist"
  }
}
